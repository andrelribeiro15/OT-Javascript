/* 1
const valoresAleatorios = [42, 'texto', true, 3.14, { nome: 'João', idade: 25 }];

console.log(valoresAleatorios);
*/

/*2
function retornarArray(array) {
    return array;
  }
  

  const meuArray = [10, 20, 30, 40, 50];
  const resultado = retornarArray(meuArray);
  
  console.log(resultado); // Saída: [10, 20, 30, 40, 50]
  
*/
// 3 - saida 30

/* 4
function obterValorPorIndice(array, indice) {
    return array[indice];
  }
  
  // Exemplo de uso
  const valores = [5, 10, 15, 20, 25];
  const indice = 3;
  
  const resultado = obterValorPorIndice(valores, indice);
  console.log(resultado); // Saída: 20
*/

/* 5 
const valoresDiferentes = [42, 'texto', true, 3.14, { nome: 'Maria', idade: 30 }];

console.log(valoresDiferentes);
*/

/* 6 
42
'texto'
true
3.14
{ nome: 'Maria', idade: 30 }
*/

/*7
function book(nomeLivro) {
    
    const livros = {
      'Harry Potter': {
        quantidadePaginas: 400,
        autor: 'J.K. Rowling',
        editora: 'Rocco'
      },
      'Dom Casmurro': {
        quantidadePaginas: 300,
        autor: 'Machado de Assis',
        editora: 'Penguin'
      },
      'O Senhor dos Anéis': {
        quantidadePaginas: 1000,
        autor: 'J.R.R. Tolkien',
        editora: 'HarperCollins'
      }
    };
  
    
    if (nomeLivro) {
      return livros[nomeLivro] || 'Livro não encontrado';
    }
  
    
    return livros;
  }
  
  
  console.log(book('Harry Potter')); 
  console.log(book()); 

*/

